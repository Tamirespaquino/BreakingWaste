//const { DatabaseError } = require("sequelize/types");

const config = {
    "development":{
        username:'root',
        password:'',
        database:'node.js',
        host:'localhost',
        dialect:'mysql',
    }
}

module.exports = config