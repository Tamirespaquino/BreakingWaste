const { DatabaseError } = require("sequelize/types");

const config = {
    username:'root',
    password:'',
    database:'node.js',
    host:'localhost',
    dialect:'mysql',
}

module.exports = config